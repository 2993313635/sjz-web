<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boshuo.sjzgl.mapper.GameCategoryMapper">

    <resultMap id="BaseResultMap" type="com.boshuo.sjzgl.model.entity.GameCategory">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, status, created_at, updated_at
    </sql>

    <!-- 插入分类 -->
    <insert id="insert" parameterType="com.boshuo.sjzgl.model.entity.GameCategory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO game_categories (name, status, created_at, updated_at)
        VALUES (#{name}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <!-- 更新分类 -->
    <update id="update" parameterType="com.boshuo.sjzgl.model.entity.GameCategory">
        UPDATE game_categories
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="status != null">status = #{status},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据ID删除 -->
    <delete id="deleteById">
        DELETE FROM game_categories WHERE id = #{id}
    </delete>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM game_categories
        WHERE id = #{id}
    </select>

    <!-- 查询所有分类 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM game_categories
        ORDER BY created_at ASC
    </select>

    <!-- 查询活跃分类 -->
    <select id="selectActiveCategories" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM game_categories
        WHERE status = 'A'
        ORDER BY created_at ASC
    </select>

</mapper>